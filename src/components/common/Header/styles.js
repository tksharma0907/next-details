const styles = {
  wrapper: {
    width: "100%",
    height: "70px",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
  },

  leftSection: {
    display: "flex",
    gap: "15px",
  },

  icon: {
    ":hover": { color: "var(--primary)" },
    cursor: "pointer",
  },
};

export default styles;
